#!/bin/bash
# one of miyoko's bash functions to manage initscripts
# its just in script form for SystemCP :P
# also, its moderately sad that the comments are twenty times
# longer than the executable code is itself.

/etc/init.d/$1 $2
